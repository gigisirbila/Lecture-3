import { RandomDirective } from './randomDirective';

describe('Random', () => {
  it('should create an instance', () => {
    const directive = new RandomDirective();
    expect(directive).toBeTruthy();
  });
});
